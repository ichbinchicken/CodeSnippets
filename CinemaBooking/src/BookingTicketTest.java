import org.junit.Assert;
import org.junit.Test;

import java.awt.print.Book;

import static org.junit.Assert.*;

public class BookingTicketTest {

    @Test
    public void getSeatsInARow() {
        /*BookingTicket t1 = new BookingTicket(1, 1, "t1", "Z1-Z50");
        BookingTicket t2 = new BookingTicket(2,2, "t2", "XXY40-XXY50");
        BookingTicket t3 = new BookingTicket(3, 3, "t3", "U5");

        Assert.assertNull(t1.getSeatsInARow(""));
        Assert.assertNull(t2.getSeatsInARow(""));
        Assert.assertNull(t3.getSeatsInARow(""));

        t1.getSeatsInARow("Z");
        t2.getSeatsInARow("XXY");
        t3.getSeatsInARow("U");

        Assert.assertFalse(t1.equals(t2));
        Assert.assertFalse(t1.equals(t3));
        Assert.assertFalse(t2.equals(t3));
        Assert.assertTrue(t1.equals(t1));

        BookingTicket t4 = new BookingTicket(1,1, "t4", "Z1-Z50");
        BookingTicket t5 = new BookingTicket(1,1,"t1", "Z1-Z50");

        Assert.assertFalse(t4.equals(t5));
        Assert.assertTrue(t1.equals(t5));*/

    }
}
